import {Component,inject,signal} from '@angular/core';import {ActivatedRoute,RouterLink} from '@angular/router';import {ApiService} from '../core/api.service';
@Component({standalone:true,imports:[RouterLink],template:`<section class="form-page"><div class="form-card"><span class="eyebrow">EMAIL CONFIRMATION</span><h1>{{message()}}</h1><a class="btn" routerLink="/login">Go to login</a></div></section>`})export class ConfirmComponent{api=inject(ApiService);route=inject(ActivatedRoute);message=signal('Confirming your email...');constructor(){this.api.confirm(this.route.snapshot.queryParamMap.get('token')||'').subscribe({next:r=>this.message.set(r.message),error:e=>this.message.set(e.error?.message||'Confirmation failed')})}}
