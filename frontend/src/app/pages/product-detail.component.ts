import {Component,inject,signal} from '@angular/core';import {ActivatedRoute,Router,RouterLink} from '@angular/router';import {CurrencyPipe} from '@angular/common';import {ApiService} from '../core/api.service';import {Product} from '../models';
@Component({standalone:true,imports:[CurrencyPipe,RouterLink],template:`@if(product();as p){<section class="detail"><div class="detail-image"><img [src]="p.image" [alt]="p.name"></div><div class="detail-copy"><a routerLink="/products">← Back to collection</a><span class="eyebrow">{{p.category}}</span><h1>{{p.name}}</h1><div class="rating">★ {{p.rating}} / 5</div><h2>{{p.price|currency}}</h2><p>{{p.description}}</p><p class="stock">● {{p.stock}} in stock</p><button class="btn wide" (click)="add(p)">Add to bag</button><small>Free delivery over $150 · 30-day returns · Secure checkout</small></div></section>}`})export class ProductDetailComponent{api=inject(ApiService);route=inject(ActivatedRoute);router=inject(Router);product=signal<Product|null>(null);constructor(){this.api.product(this.route.snapshot.paramMap.get('id')!).subscribe(v=>this.product.set(v))}add(p:Product){if(!this.api.user()){this.router.navigateByUrl('/login');return}this.api.add(p._id).subscribe(()=>this.router.navigateByUrl('/cart'))}}
