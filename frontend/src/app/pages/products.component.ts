import {Component,inject,signal} from '@angular/core';import {CurrencyPipe} from '@angular/common';import {RouterLink} from '@angular/router';import {FormsModule} from '@angular/forms';import {ApiService} from '../core/api.service';import {Product} from '../models';
@Component({standalone:true,imports:[CurrencyPipe,RouterLink,FormsModule],template:`<section class="page-intro"><span class="eyebrow">THE COLLECTION</span><h1>Find your next favorite.</h1><p>Smart, beautiful essentials selected for modern life.</p></section><section class="catalog"><aside><input [(ngModel)]="search" (ngModelChange)="load()" placeholder="Search products"><button [class.selected]="!category" (click)="category='';load()">All</button>@for(c of categories;track c){<button [class.selected]="category===c" (click)="category=c;load()">{{c}}</button>}</aside><div><div class="results">{{products().length}} products</div><div class="product-grid">@for(p of products();track p._id){<article class="product-card"><a [routerLink]="['/products',p.slug]"><div class="product-image"><img [src]="p.image" [alt]="p.name"></div><p>{{p.category}}</p><h3>{{p.name}}</h3><b>{{p.price|currency}}</b></a></article>}@empty{<p>No products found.</p>}</div></div></section>`})export class ProductsComponent{api=inject(ApiService);products=signal<Product[]>([]);search='';category='';categories=['Audio','Wearables','Cameras','Accessories','Computers'];constructor(){this.load()}load(){this.api.products({...(this.search&&{search:this.search}),...(this.category&&{category:this.category})}).subscribe(v=>this.products.set(v))}}
